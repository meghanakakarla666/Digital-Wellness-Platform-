document.addEventListener('DOMContentLoaded', function() {
    // Initialize the wellness platform
    initializePlatform();
    
    // Add smooth scrolling for navigation links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth'
                });
            }
        });
    });

    // Add animation to cards on scroll
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver(function(entries) {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0)';
            }
        });
    }, observerOptions);

    // Observe all health cards
    document.querySelectorAll('.health-card').forEach(card => {
        card.style.opacity = '0';
        card.style.transform = 'translateY(20px)';
        card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
        observer.observe(card);
    });
});

function initializePlatform() {
    console.log('Digital Wellness Platform initialized');
    
    // Check if user has previous assessment data
    const previousData = localStorage.getItem('wellnessAssessment');
    if (previousData) {
        console.log('Previous assessment data found');
        // Could show a "Continue Assessment" option
    }
}